*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties
*.aab
keystore.properties
signing/
signing.properties.bak

# Keep: release.keystore, signing.properties, apk/*.apk (archived)
# Do NOT ignore these — they must be committed for CI reproducible builds
!release.keystore
!signing.properties
