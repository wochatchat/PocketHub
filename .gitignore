*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties
*.aab
keystore.properties
signing/
signing.properties.bak

# Release APKs are generated artifacts. Keep them out of Git history;
# build.yml uploads the generated APK directly to the GitHub Release.
apk/*.apk

# CI signing inputs remain tracked for the existing release pipeline.
!release.keystore
!signing.properties
